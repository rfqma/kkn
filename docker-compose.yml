version: '3.8'

services:
  app:
    image: rfqma/kkn:latest
    platform: linux/amd64
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    ports:
      - "3005:3000"
    restart: always